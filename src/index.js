import React from "react";
import ReactDOM from "react-dom/client";
import RenderProfile from "./RenderProfile";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(<RenderProfile />);
